dist: trusty
sudo: false
language: cpp
os:
  - linux
addons:
  apt:
    packages:
      - build-essential
      - scons
      - pkg-config
      - libgles2-mesa-dev
      - libasound2-dev
script:
  - make ci
notifications:
  email: false
